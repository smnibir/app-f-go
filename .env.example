# --- Required in production ---
# Supabase: Project Settings → Database → use "Connection pooling" URI (port 6543).
# Append: ?sslmode=require&pgbouncer=true
DATABASE_URL="postgresql://USER:PASSWORD@HOST:6543/postgres?sslmode=require&pgbouncer=true"

# openssl rand -base64 32
AUTH_SECRET=""

# Public URL of the app (include https in production)
NEXTAUTH_URL="https://your-domain.com"

# --- Seed (first deploy / local only) ---
# SEED_SUPER_ADMIN_EMAIL="admin@example.com"
# SEED_SUPER_ADMIN_PASSWORD="change-me-after-first-login"

# --- Optional ---
# NEXTAUTH_SECRET=""
# AUTH_URL=""
# SENDGRID_API_KEY=""
# SENDGRID_FROM_EMAIL=""
# NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
# CLOUDINARY_API_KEY=""
# CLOUDINARY_API_SECRET=""
